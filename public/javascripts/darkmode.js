function toggleDarkMode() {
    document.body.classList.toggle("dark-mode");
}

function startHabit(habitName) {
    alert(`Starting habit: ${habitName}...`);
    // TODO: Redirect or load levels page
    // window.location.href = `/habit/${habitName.toLowerCase()}`;
}
  